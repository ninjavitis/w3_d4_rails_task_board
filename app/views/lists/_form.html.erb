<%= form_with(model:[@board, @list], local:true, class:"ui form") do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, autofocus:true, required:true, placeholder:"Primary Objective..."%>
  </div>
  <%= f.submit nil, class:"mini green ui button"%>
<% end %>